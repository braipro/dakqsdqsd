{
  "name": "rtmp-streamer",
  "version": "1.0.0",
  "description": "خادم بث مباشر عبر Telegram",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.61.0",
    "fluent-ffmpeg": "^2.1.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
